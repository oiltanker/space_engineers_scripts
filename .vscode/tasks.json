{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "py build script",
			"type": "shell",
			"command": "python",
			"args": [ "${workspaceFolder}/__build_script__.py", "--root=\"${workspaceFolder}\"", "--file=\"${file}\"" ]
		}
	]
}